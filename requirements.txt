# =============================
# LLM / AI (이미지 분석 & 리뷰 생성)
# =============================
anthropic               # Claude Vision으로 이미지 → 리뷰 텍스트 생성
openai                  # GPT-4o Vision 대체 사용 시

# =============================
# 이미지 처리
# =============================
Pillow                  # 이미지 리사이즈, 포맷 변환
python-multipart        # 이미지 파일 업로드 처리

# =============================
# 네이버 블로그 자동화
# =============================
selenium                # 네이버 블로그 포스팅 자동화 (Naver API 미지원 우회)
webdriver-manager       # ChromeDriver 자동 설치/관리
playwright              # selenium 대안 (더 안정적인 브라우저 자동화)

# =============================
# HTTP 요청 / 네이버 Open API
# =============================
requests                # 네이버 Open API 호출
httpx                   # 비동기 HTTP 클라이언트

# =============================
# 웹 서버 (이미지 업로드 UI)
# =============================
fastapi                 # 이미지 업로드 받는 백엔드 서버
uvicorn[standard]       # FastAPI ASGI 서버

# =============================
# 환경변수 / 설정 관리
# =============================
python-dotenv           # .env 파일로 API Key 관리
pydantic                # 설정값 유효성 검증
pydantic-settings       # BaseSettings로 환경변수 관리

# =============================
# 유틸리티
# =============================
aiofiles                # 비동기 파일 I/O
loguru                  # 로깅
tenacity                # API 호출 재시도 로직
